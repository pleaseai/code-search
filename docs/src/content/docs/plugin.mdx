---
title: Plugin (Claude Code & Codex)
description: Install the csp plugin to register the MCP server and the csp-search helper in one step.
---

The fastest way to set up `csp` is the plugin, which registers the MCP server **and** the `csp-search` helper in one step. It ships for both Claude Code and Codex from the same directory.

<Tabs>
  <TabItem label="Claude Code">
    ```text
    /plugin marketplace add pleaseai/claude-code-plugins
    /plugin install csp@pleaseai
    ```

    Then restart Claude Code, or run `/mcp` to confirm the `csp` server is connected.
  </TabItem>
  <TabItem label="Codex">
    ```bash
    codex plugin marketplace add pleaseai/claude-code-plugins
    codex plugin add csp@pleaseai
    ```
  </TabItem>
</Tabs>

## What the plugin installs

- **MCP server `csp`**: exposes the `search` and `find_related` tools, launched via `bunx @pleaseai/csp mcp`. Repos are cloned and indexed on demand; local paths are watched and re-indexed automatically.
- **`csp-search` helper**: a dedicated Claude Code sub-agent (`agents/csp-search.md`) or Codex skill (`skills/csp-search/SKILL.md`) that runs `csp` searches with the right flags.

## Requirements

[Bun](https://bun.sh) 1.3.10+ must be on your `PATH` so `bunx` can launch the MCP server. Node.js 22+ also works via `npx @pleaseai/csp mcp` (edit the MCP config if you prefer npm).

## Customization

By default the MCP server indexes only code files. To also index documentation or config, append `--content` to the server args, for example `["@pleaseai/csp", "mcp", "--content", "all"]`. A single tool call can also override this with its `content` argument (`code`, `docs`, `config`, or `all`).

## Layout

One directory carries both manifests; each harness reads its own and ignores the other.

| | Claude Code | Codex |
|---|---|---|
| Manifest | `.claude-plugin/plugin.json` | `.codex-plugin/plugin.json` |
| MCP config | `.mcp.json` (`mcpServers`) | `.mcp.codex.json` (`mcp_servers`) |
| Helper | `agents/csp-search.md` (sub-agent) | `skills/csp-search/SKILL.md` (skill) |

The plugin source lives at [plugins/csp](https://github.com/pleaseai/code-search/tree/main/plugins/csp) in the repository.
